#pragma once

#include "glm/glm.hpp"

// Instead of wrting glm::dvec3 everywhere and also making it more clear what is a direction vs a vertex vs a color, some definitions are set up
using Vertex = glm::dvec3;
using Direction = glm::dvec3;
using Color = glm::dvec3;
